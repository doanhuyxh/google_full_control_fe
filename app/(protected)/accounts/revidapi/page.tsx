import RevidApiComponent from "@/components/features/revidapi";

export const metadata = {
    title: "RevID API",
    description: "Manage your RevID API settings and access.",
};

export default function RevidApiPage() {
    return (
        <RevidApiComponent />
    );
}