{
    "github": {
        "silent": true
    },
    "ignoreCommand": "bash -c '[ \"$VERCEL_GIT_COMMIT_REF\" != \"master\" ]'",
    "cron": {
        "path": "/api/cron",
        "schedule": "* * * * *"
    }
}